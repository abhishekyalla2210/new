- name: printing the loops
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: printing the loops
    ansible.builtin.package:
      name: "{{ item.name }}"
      state: "{{ item.state }}"
    loop:
    - { name: "nginx", state: "absent" }
    - { name: "mysql", state: " absent " }
 